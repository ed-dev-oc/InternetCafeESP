#include "local_credentials.h"

// Template for local credentials - copy this to local_credentials.cpp
// and fill in your actual values

// WiFi Credentials
const char* WIFI_SSID = "YourNetworkName";
const char* WIFI_PASSWORD = "YourSecretPassword";

// Device & Server Configuration
const char* DEVICE_NAME = "COIN-SLOT-01";
const char* SERVER_URL = "http://your-server-ip:3000";